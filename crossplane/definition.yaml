apiVersion: apiextensions.crossplane.io/v1
kind: CompositeResourceDefinition
metadata:
  name: xvellumplatforms.infrastructure.vellum.io
spec:
  group: infrastructure.vellum.io
  names:
    kind: XVellumPlatform
    plural: xvellumplatforms
  claimNames:
    kind: VellumPlatform
    plural: vellumplatforms
  versions:
  - name: v1alpha1
    served: true
    referenceable: true
    schema:
      openAPIV3Schema:
        type: object
        properties:
          spec:
            type: object
            properties:
              parameters:
                type: object
                properties:
                  version:
                    type: string
                    description: "Version of the platform to install"
                    default: "v0.1.0"
                required:
                - version
            required:
            - parameters
